# NPM Configuration for Publishing
registry=https://registry.npmjs.org/
access=public

# Ensure proper package publishing
package-lock=true
save-exact=false

# Publishing configuration
# This ensures the package is published as public
